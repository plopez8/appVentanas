let ROL = "null"; // Declara la variable como let para que sea mutable

export const getROL = () => ROL; // Función para obtener el valor de ROL

export const setROL = (newValue) => { // Función para modificar el valor de ROL
  ROL = newValue;
};